## Important note
To run the program:
* First run `venv\Scripts\activate` to activate the virtual environment. Recommended to run on **command terminal** instead of Powershell.
* Then, run the program with `python run.py`. To end the program use the `Ctrl+C` keybind.
---

